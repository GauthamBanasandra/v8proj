function exponent(base, exp)
{
    var result = 1;
    x = y;
    for (var i=0; i < exp; i++) {
        result *= base;
    }
    return result;
}

function exponent2(params) {
	console.log(params);
}